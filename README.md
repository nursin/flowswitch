# Flow Switch (CRA + Tailwind)

A Create React App project wired with Tailwind CSS and CRA aliases so you can import from `@/`.

## Quick start

```bash
npm install
npm start
```
This runs at http://localhost:3000

## Build
```bash
npm run build
```
# flowswitch
